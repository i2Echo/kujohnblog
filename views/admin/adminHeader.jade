header.navbar.navbar-inverse.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#adbs-navbar')
        span.sr-only Toggle Navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      .head-mate.clearfix
          a.navbar-brand(href='/') Kujohn Blog
    .collapse.navbar-collapse#adbs-navbar
      ul.nav.navbar-nav.text-center
        li(class="#{}")
          a(href='#') Articles
        li(class="#{}")
          a(href='#') Users
        li(class="#{}")
          a(href='#') Comments
        li(class="#{}")
          a(href='#') Settings

  if (success)
    .alert.alert-success#alertMessage
      a.close(data-dismiss="alert") &times;
      strong #{success}
  if (error)
    .alert.alert-danger#alertMessage
      a.close(data-dismiss="alert") &times;
      strong #{error}